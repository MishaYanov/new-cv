import { component$, useStore } from "@builder.io/qwik";


export default component$(() => {
    // useStore={(
    //     image: this.props.first
    // )}


  return (
    <div className=""></div>
  );
});
